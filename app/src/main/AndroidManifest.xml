<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.priscilanathalie.agenda_pris_jaz">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Agenda"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Agregar"></activity>
        <activity android:name=".Eliminar"></activity>
        <activity android:name=".Buscar"></activity>
        <activity android:name=".Modificar"></activity>
        <activity android:name=".Id_Modificar"></activity>
    </application>

</manifest>